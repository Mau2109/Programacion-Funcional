(define (factorial n)
  (if 
     (eq? n 0)
        1
        (* n (factorial (- n 1)))
        )
  )

(define (abs1 n)
  (if (< n 0)(- n) n)
  )

(define (abs2 n)
  (if (or) > n 0) (eq? n 0)) n (-n)))
